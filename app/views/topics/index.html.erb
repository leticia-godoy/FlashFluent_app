<%= render 'shared/page_title', title: "Principais Tópicos"%>

<div align="center">
    <%= will_paginate @topics, previous_label: '<- Anterior', next_label: 'Próxima ->' %>
    <%@topics.each do |tpc|%>
    <ul class="listing">
        <div class="row">
            <div class="well col-md-4 col-md-offset-4">
                <li class="user-title"><%=link_to tpc.name.capitalize, topic_path(tpc)%></li>
                <li><small><%= pluralize(tpc.cards.count, "card") if tpc.cards%></small></li>
                <% if logged_in? && current_user.instructor? %>
                <li><%= link_to "Editar tópico", edit_topic_path(tpc)%></li>
                <% end %>
            </div>
        </div>
    </ul>
    <% end %>
    <%= will_paginate @topics, previous_label: '<- Anterior', next_label: 'Próxima ->' %>
</div>