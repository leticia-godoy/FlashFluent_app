<%= render 'shared/page_title', title: "#{@user.username}"%>

<div class="row">
    <div class= "col-md-4 col-md-offset-4 center">
        <%= gravatar_for @user, size: 150%>
    </div>
</div>

<h4 align="center">FlashCards do(a) <%=@user.username%></h4><br>
<%= render 'cards/card', obj: @user.cards%>